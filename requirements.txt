# Core AWS dependencies
boto3>=1.34.0
botocore>=1.34.0

# AWS CDK dependencies
aws-cdk-lib>=2.100.0
constructs>=10.0.0

# Lambda runtime dependencies
requests>=2.31.0
python-dateutil>=2.8.2

# Development dependencies
pytest>=7.4.0
pytest-mock>=3.11.0
moto>=4.2.0

# MCP server dependencies (for development)
# Note: MCP servers will be deployed separately
asyncio-mqtt>=0.13.0
pydantic>=2.0.0